import type { DefaultValues } from '$lib/types'

export const defaultValues: DefaultValues = {
    avgCollectionCost: 10,
    taxesReserved: 30, // percent
    txTaxes: 10,// percent
    personalReserve: 2.5, // percent
    nftCost: 25,
    maxNftsPerWallet: 25,
    debtTokenPrice: 0,
    stakedDebtTokens: 0,
}